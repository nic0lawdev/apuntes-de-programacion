para declara una variable utilizaremos una anotacion llamada camelCase ejemploNombreVariable
(cada vez que comienze una nueva palabra comienza con letra mayuscula)
 
para declarar una variable utilizaremos las palabras claves

//Las variables declaradas con var tienen un alcance de función o global,
var nombreVarible = " NottNico";

//let permite declarar variables con un alcance de bloque.
let nombreVarible2 = "nottnicolaw";

//const se utiliza para declarar constantes, es decir, variables que no pueden cambiar su valor una vez asignado.
const emailConstante = "NottNicolaw@gmail.com"

let x = 10;
let y = 5;

let suma = x + y ;

console.log(suma);

expresiones aritméticas

let x = 5;
let y = 3;

let suma = x + y; // Suma: 8
let resta = x - y; // Resta: 2
let multiplicacion = x * y; // Multiplicación: 15
let division = x / y; // División: 1.6666666666666667
let resto = x % y; // Resto: 2
let exponente = x ** y; // Exponente: 125

let numero = 100;
console.log(numero)//Salida por consola --> 100
//incremento
numero++;
console.log(numero)//Salida por consola --> 101
//decremento
numero = numero--;
console.log(numero)//Salida por consola --> 100


Operadores de Asignación

let z = 10;
z += 5; // Equivale a z = z + 5; (z = 15)
z -= 3; // Equivale a z = z - 3; (z = 12)
z *= 2; // Equivale a z = z * 2; (z = 24)
z /= 4; // Equivale a z = z / 4; (z = 6)
z %= 2; // Equivale a z = z % 2; (z = 0)



Operadores de comparacion
igualdad (==)
igualdad estricta (===)
desigualdad (!=)
desigualdad estricta(!==)

let x = 5;
let y = 10;

console.log(x > y); // mayor que
console.log(x < y); // menor que
console.log(x >= y); // mayor o igual que
console.log(x <= y); // menor o igual que


Operadores Lógicos


Operador AND (&&):
let x = 5;
let y = 10;

console.log(x > 0 && y > 0); //  si x es mayor que 0 e y es mayor que 0 devuelve true
console.log(x > 0 && y < 0); //  si x es mayor que 0 e y es menor que 0 devuelve false

Operador OR (||):

let x = 5;

Pág. 19

let y = 10;

console.log(x > 0 || y > 0); // Devuelve true
console.log(x < 0 || y < 0); // Devuelve false

Operador NOT (!):
let x = 5;
let y = 10;

console.log(!(x > 0)); // Devuelve false
console.log(!(y < 0)); // Devuelve true

Sentencias condicionales


// Variable que indica si el usuario está logueado
let usuarioLogueado = true;

if (usuarioLogueado) {
// Si el usuario está logueado, se muestra el contenido para usuarios
logueados
console.log("Bienvenido, usuario logueado. Acá está el contenido exclusivo.");
} else {
// Si el usuario no está logueado, se muestra el contenido para usuarios no
logueados
console.log("Por favor, inicia sesión para acceder a este contenido.");
}

let edad = 18;

if (edad >= 18) {
console.log("Eres mayor de edad");
}


Operador AND (&&) con IF/ELSE:

let edad = 25;
let tieneLicencia = true;

if (edad >= 18 && tieneLicencia) {
console.log("puedes conducir legalmente.");
} else {
console.log("No cumplis con los requisitos para conducir.");
}

Operador OR (||) con IF/ELSE:

let esEstudiante = false;
let esEmpleado = true;

if (esEstudiante || esEmpleado) {
console.log("Tenes acceso a descuentos especiales.");
} else {
console.log("No tenes acceso a descuentos especiales.");
}


Operador Ternario
Su sintaxis es la siguiente:
condicion ? expresion1 : expresion2;

let num1 = 7;
let num2 = 5;

//Operador ternario: (condición ? verdadero : falso)

let resultado = num1 > num2 ? "el 1" : "el 2";

console.log(resultado);


switch en JavaScript se utiliza cuando se necesita evaluar una
expresión y ejecutar diferentes bloques de código



function saludar(){
    console.log("hola gente los estoy saludando a traves de la consola  ")
}

switch (expresion) {
case valor1:
// Bloque de código a ejecutar si la expresion es igual a valor1
break;
case valor2:
// Bloque de código a ejecutar si la expresion es igual a valor2
break;
case valor3:
// Bloque de código a ejecutar si la expresion es igual a valor3
break;
// ...
default:
// Bloque de código a ejecutar si ninguno de los casos anteriores se cumple
}



las funciones tienen el fin de evitar la constante 
repeticion de bloques de codigo

entonces como resumen las funciones son un conjunto de intruccione, que
transforma en un bloque de codigo el cual es reutilizable



las principales ventajas son

evitar  intrucciones repetitivas
aporta ordenamiento y entendimiento al codigo

la declaracion se realiza con la palabra 
function nameFuncion(){

    bloque de codigo
}


function conParametros(name, surname, lastname){

    console.log('saludar a' + name + ' como estas'+)
}

conParametros(); // ejecutando la function

una variable definida dentro de la funcion es una varible local que solo va 
estar disponible dentro de este bloque

y una variable definida fuera de una funcion es una variable global

Scope

es la zona del programa en la cual se define
pueden ser global o local


let nombre = "Juan"; // escope global

function saludar() {
    let nombre = "pedrito"
    console.log("Hola, " + nombre); // Accediendo a la variable global dentro de la función
}

saludar(); // escope local
console.log(nombre); // Imprime "Juan"


funciones anonimas


Una función anónima es una función que se define sin nombre y se utiliza para
ser pasada como parámetro o asignada a una variable.

const varible = function (a,b){ return a+b}

console.log( suma(15,20) )


Funciones Flechas =>



const suma = (a, b) => { return a + b }

Si la función flecha tiene un solo parámetro, se pueden omitir los paréntesis
alrededor del parámetro.

Funcion flecha sin con un parametro



const duplicar = numero => {
return numero * 2;
};



quede en obtener datos de inputs

